# Provider-window comparison review — 11 September 2026

This is an operator diagnostic, not an application pipeline change. The experiment is **inconclusive**. It does not establish provider-offset timestamp semantics, representative footage accuracy, comparative throughput, or measured generation cost.

## Every review finding and disposition

Paths and lines below identify the final code relevant to each finding. Duplicate findings from independent reviewers are retained.

1. **Simplify / reuse — Duplicated provider reservation amount.** [compare_video_windows.py:168](../scripts/compare_video_windows.py#L168). Fixed: report the shared `CALL_ALLOWANCE["gemini"]` value used by application reservations instead of a second hardcoded 60,000-microdollar value.
2. **Simplify / reuse — Unbounded duplicate FFmpeg execution.** [compare_video_windows.py:62](../scripts/compare_video_windows.py#L62). Fixed: use the existing `run_media` helper with a 60-second timeout for each generated fixture, retaining the established subprocess diagnostic and cleanup behavior.
3. **Simplify / quality — Report existence check could race concurrent paid runs.** [compare_video_windows.py:53](../scripts/compare_video_windows.py#L53). Fixed: exclusively create and flush the initial report before any media or provider work. A competing run or ordinary rerun fails before a reservation. Later report writes replace the file atomically.
4. **Simplify / quality — Duplicate 60,000-microdollar constant.** [compare_video_windows.py:168](../scripts/compare_video_windows.py#L168). Fixed by the same shared allowance reference as finding 1; retained as a separate reviewer finding.
5. **Simplify / efficiency — Inventory bound reads one extra item.** [compare_video_windows.py:281](../scripts/compare_video_windows.py#L281). Retained minor limitation: verifying deletion may retrieve item 1,001 and trigger an eleventh page before refusing an oversized inventory. It is bounded and outside the application hot path; no strict 1,000-item network-read guarantee is claimed.
6. **Final / breaking changes — Missing usage could still produce a passing report.** [compare_video_windows.py:245](../scripts/compare_video_windows.py#L245). Fixed: both methods require positive input, candidate and total token counts, with total covering input, candidates and any reported thinking. Missing/inconsistent usage cannot establish a pass.
7. **Final / testing — Missing usage could still produce a passing report.** [compare_video_windows.py:298](../scripts/compare_video_windows.py#L298). Fixed and retained independently from finding 6. Reviewer exercised ten isolated complete runs covering valid/missing/inconsistent usage, extra fields, invalid bounds/order/colors/timing and excessive intervals; only the valid case passed, and cleanup remained verified.
8. **Final / testing and SDK diagnosis — Dispatch flag preceded SDK conversion.** [compare_video_windows.py:134](../scripts/compare_video_windows.py#L134). Fixed: separate attempted generation from the HTTP request hook that records dispatch start. Offline reproduction of the original schema error saw zero HTTP requests; the original report's early `generation_dispatched` flag is explicitly not evidence of transport activity.
9. **Final / context — P0 manual-review flag for a video item potentially above 1,000 tokens.** [compare_video_windows.py:182](../scripts/compare_video_windows.py#L182). Manual review completed and accepted: the only inputs are fixed generated 12-second/six-second videos and a short fixed prompt, with a 5,000-token aggregate preflight and 512-token output bound. The skill does not require additional user permission. The actual preflight was 204 tokens; it is not successful-generation evidence. No application context/history mutation occurs; the Rust `core/context` trait rule is N/A to this standalone Python diagnostic.
10. **Parent validation — Provider-schema conversion rejected `exclusiveMinimum`.** [compare_video_windows.py:212](../scripts/compare_video_windows.py#L212). Fixed by sending a portable explicit wire schema while retaining strict local Pydantic validation and positive ordered intervals. A real SDK with an offline HTTP transport now covers conversion. The later HTTP 400 had no captured error message, so its cause is not claimed proven.
11. **Parent validation — Diagnostic generation timeout was shorter than the application timeout.** [compare_video_windows.py:209](../scripts/compare_video_windows.py#L209). Fixed for future experiments: generation gets 120 seconds, while file and count requests retain 30 seconds. The prior HTTP 504 said the deadline expired, but a longer timeout has not been live-validated and is not guaranteed to resolve it. The uncertain generation was not retried.
12. **Final follow-up / breaking changes — Mock offset assertions never executed.** [test_video_window_diagnostic.py:65](../tests/test_video_window_diagnostic.py#L65). Fixed: the installed SDK serializes the metadata keys as `start_offset`/`end_offset`; the test had checked camelCase, so both mocked responses used zero-relative timestamps. The first request now must contain exactly `4s`/`10s`, the second must omit offsets, and the successful report must recognize the first absolute interval and second relative interval. The invalid-interval case's dispatch assertion accounts for rejection by classification first and strict schema validation on the second method.

The change-size reviewer reported no finding for the original bounded diagnostic snapshot. The final follow-up reviewer confirmed the usage and offset-assertion fixes with no new finding. No GitHub PR exists to label; no review comments were posted.

## Actual provider attempts

| Attempt | Result | Generation usage | Cleanup |
| --- | --- | --- | --- |
| [Initial](validation/provider-window-comparison-initial.json) | SDK schema conversion error; matching offline reproduction dispatched zero HTTP requests | Unavailable | Deleted and absence verified |
| [Rejected](validation/provider-window-comparison-rejected.json) | HTTP 400; server message was not captured | Unavailable | Deleted and absence verified |
| [Latest](validation/provider-window-comparison.json) | HTTP 504, deadline expired, total 31.868 seconds | Unavailable; possible charge remains uncertain | Deleted and absence verified |

Only the full-proxy method was attempted; each failure stopped the second method. Each uploaded file was the same 9,465-byte generated fixture, SHA-256 `04759d2cb3773838ca4158090781c90b6de8354c5e5c434e793471f7fc85d102`. The measured 204 preflight tokens do not identify billed generation tokens. Three $0.06 reservations ($0.18 total) remain retained in the local configured allowance. They are separate from the production application ledger and actual cloud invoices. No camera originals or user account details were uploaded by this diagnostic.

## Validation and future execution

Five committed offline tests in [test_video_window_diagnostic.py](../tests/test_video_window_diagnostic.py) passed in 1.22 seconds after the offset assertion repair. They use the actual Google SDK with a mock HTTP transport, verify portable schema serialization, request timeouts, source offsets, strict usage/timing acceptance, single dispatch on HTTP 504, cleanup, and refusal to repeat an existing report. Files, media execution and reservations are isolated; no provider, database or production calls occur. Ruff check and formatting passed for both diagnostic and tests.

An existing report deliberately prevents another paid run. Before a future explicitly initiated experiment, inspect and preserve that report, account for ambiguous prior exposure, and choose a sufficient remaining project allowance. The script requires private Gemini configuration and an explicit allowance in the configured local database. It never turns the production allowance or cloud invoices into a global hard cap. Do not rename a report merely to retry automatically after an uncertain request.

[Official Gemini video metadata documentation](https://ai.google.dev/gemini-api/docs/generate-content/video-understanding) documents clipping offsets. It does not replace live verification of the response timestamp convention or performance of this particular model/SDK configuration.
