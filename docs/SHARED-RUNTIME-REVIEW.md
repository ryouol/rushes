# Shared hosted runtime review — 12 September 2026

This is a reviewed local candidate, not a deployment or a $20 invoice cap. Production remains `3584971` on the existing $35/month fixed topology. The candidate shares Python imports between the hosted API and its Temporal workers, limits workflow execution to four slots/eight cached workflows, and lowers the private Go memory target to 128 MiB. Separate local/API-only operation is preserved.

Three simplify perspectives and all four final code-review skills reviewed the changes. Issues and recommendations returned by those reviewers are retained below, including overlapping findings. No pull request, review comments, or GitHub label were created.

1. **Fixed — Prototype quality: readiness preceded namespace validation.** [worker.py:136](../backend/rushes/worker.py#L136) now waits for the SDK's public running state and observes task failures. Explicit run tasks avoid the prototype's SDK context-manager startup hang.
2. **Fixed — Prototype quality: the shutdown budget omitted HTTP drain.** [serve.py:125](../scripts/serve.py#L125) allows 50 seconds for hosted HTTP/worker shutdown before a process-group hard stop; Temporal stops last, with a further 15-second limit.
3. **Fixed — Prototype quality: standalone worker CLI disappeared.** [worker.py:211](../backend/rushes/worker.py#L211) retains signal-driven standalone execution for local and external-worker setups.
4. **Fixed — Prototype reuse: API-only mode started workers unconditionally.** [api_lifespan.py:14](../backend/rushes/api_lifespan.py#L14) gates worker startup on the existing hosted-workflow setting.
5. **Fixed — Quality: synchronous export writes would block shared HTTP handling.** [export_documents.py:53](../backend/rushes/export_documents.py#L53) contains guarded-thread writers. Worklogs retain bounded 200-row reads; JSON separators, CSV formula escaping and completion receipts are preserved.
6. **Fixed — Reuse: two tests duplicated the blocked validation factory.** [test_worker_lifecycle.py:62](../tests/test_worker_lifecycle.py#L62) provides the shared fixture.
7. **Fixed — Quality: a failed worker could cancel its healthy companion before draining.** [worker.py:192](../backend/rushes/worker.py#L192) gathers both shutdown outcomes before raising either failure. The regression includes an SDK-like failure after drain completion.
8. **Fixed — Efficiency: an unbounded final gather defeated the shutdown deadline.** [worker.py:197](../backend/rushes/worker.py#L197) uses a bounded wait and consumes eventual task results without awaiting cancellation-resistant work indefinitely. Storage leases still drain safely; the supervisor remains the final process-level deadline.
9. **Fixed — Efficiency: workflow memory defaults exceeded the activity limits.** [worker.py:168](../backend/rushes/worker.py#L168) sets four workflow slots and eight cached workflows instead of inherited defaults of 100 slots, 1,000 cached workflows and an executor allowing 500 threads. These defaults were verified in the installed SDK; this was not established as the cause of the 4K OOM.
10. **Fixed — Reuse: three document writers duplicated flush/fsync.** [export_documents.py:57](../backend/rushes/export_documents.py#L57) reuses [storage.py:21](../backend/rushes/storage.py#L21), whose annotation also accepts text streams.
11. **Fixed — Efficiency: output-path preflight still performed a burst of filesystem calls on the API loop.** [exports.py:160](../backend/rushes/exports.py#L160) groups source authorization, space checks, duplicate-name checks and path validation into one guarded storage call.
12. **Retained capacity limitation — Efficiency: one FFmpeg thread still permits two media activities.** [worker.py:167](../backend/rushes/worker.py#L167) retains two media activities. The single-video checks do not qualify concurrent 4K capacity; no smaller-plan deployment is approved by this evidence.
13. **Fixed — Final testing, overlapping preflight finding: threaded preflight also needs activity heartbeats.** [exports.py:199](../backend/rushes/exports.py#L199) wraps the complete preflight with `keep_alive`.
14. **Implemented — Final testing: internal worker failure needed a real Uvicorn boundary test.** [test_hosted_lifespan.py:11](../tests/test_hosted_lifespan.py#L11) verifies readiness, triggers a fake worker failure, and observes real lifespan cleanup, process termination and a closed socket. Only workers are replaced; Uvicorn, the application lifespan, and SIGTERM are real. A direct Uvicorn process may re-raise SIGTERM after graceful cleanup.
15. **Addressed — Final size: the combined change exceeded 800 lines.** [exports.py:160](../backend/rushes/exports.py#L160), [pipeline.py:259](../backend/rushes/pipeline.py#L259), and [api_lifespan.py:12](../backend/rushes/api_lifespan.py#L12) form ordered export, checkpoint/storage, and hosted-lifecycle stages. The real-server integration check is an additional verification stage. Each implementation stage is reviewed separately; filesystem prerequisites precede shared activity execution.
16. **Fixed — Final breaking changes: checkpoint filesystem operations remained on the shared API loop.** [pipeline.py:307](../backend/rushes/pipeline.py#L307) and [activities.py:467](../backend/rushes/activities.py#L467) move transcript/timing reads, checkpoint writes and temporary-file deletion into guarded storage threads. Analysis preparation and cleanup also retain heartbeats.
17. **Fixed — Final breaking follow-up, overlapping filesystem finding: directory creation still blocked.** [pipeline.py:127](../backend/rushes/pipeline.py#L127), `ensure_prepared`, and `transcribe_asset` now acquire their artifact folders through storage threads.
18. **Fixed — Final breaking follow-up: lease acquisition/release also needed offloading without cancellation leaks.** [storage.py:148](../backend/rushes/storage.py#L148) drains acquisition and always releases the context in a guarded thread, including when cancellation arrives before acquisition returns. [test_storage_activity.py:11](../tests/test_storage_activity.py#L11) then acquires an exclusive lease to verify release.
19. **Fixed — Final breaking follow-up: awaiting audio cleanup before saving could discard a completed transcript.** [pipeline.py:314](../backend/rushes/pipeline.py#L314) publishes the transcript before cleanup. [test_checkpoint_io.py:16](../tests/test_checkpoint_io.py#L16) interrupts both checkpoint writing and audio deletion, verifies responsive HTTP, and resumes from the saved transcript without another transcription call.
20. **Documentation disposition — Final breaking review: the earlier supervisor report names the old Go target.** [SUPERVISOR-REVIEW.md:25](SUPERVISOR-REVIEW.md#L25) is historical evidence for that deployed stage. This report records the candidate's 128 MiB target; production remains on the previous runtime until a later explicit deployment is verified.

Final context review found no new model-visible fragments or prompt/history changes. Existing Gemini transcript and preflight bounds remain unchanged; this repository has no Rust `core/context` integration. Final reuse review found no further worthwhile duplication.

The final ordered implementation commits are `45308cb` (export writers, 416 changed lines), `5f88c6f` (checkpoint/storage cancellation, 191 changed lines), and `5b40566` (hosted lifecycle, 468 changed lines). The real-Uvicorn verification adds 73 test lines in a separate evidence commit. All implementation stages remain under 500 lines. Final breaking/testing and all simplify follow-ups returned no remaining actionable issue after the fixes above.

## Verification and remaining limits

**132 isolated tests passed in 15.48 seconds**, covering actual database exports, source/path authorization, cancellation, checkpoint replay, deletion leases, provider-budget boundaries with mocked providers, worker lifecycle, and a real Uvicorn shutdown. Gemini credentials and general provider allowance were disabled for the test environment. No model generation was used. [Evidence](validation/shared-runtime.json).

A disposable 2 GiB / 1 CPU container passed a two-second 3840×2160 upload, active-work interruption/resumption, preview byte ranges, preserved session/original bytes, PostgreSQL workflow recovery, private Temporal isolation, startup termination and API/Temporal crash handling. It used current Python source mounted onto the previously built amd64 image on an arm64 host. Its sampled 571,924,480-byte peak includes an extra in-container workflow probe. Final filesystem refinements followed that harness and were verified by the isolated regression suite; this is not a fresh final-image test or representative capacity qualification.

The 512 MiB prototype passed the same small journey with 320×180 footage, then the canonical 4K candidate failed with Docker `OOMKilled=true` while resuming preparation. The tiny probe retained only the resumed container's memory peak. All three experiments' owned containers, volumes, databases and roles were independently verified removed. No Render resources or other projects were changed.

The next cost-reduction candidate is a static Next export served by a lightweight frontend process. Read-only feasibility review found the interactive routes compatible, but the API streaming proxy, security headers, runtime public metadata/contact/analytics configuration, static navigation payloads and Docker runtime must be preserved and tested explicitly. No frontend export changes were made in this stage. Current fixed cost remains above the requested **$20/month combined**; no total invoice cap is claimed. Existing seven local videos still need analysis, and the broader representative-footage/editor/legal requirements remain open in [the goal audit](GOAL-AUDIT.md).
