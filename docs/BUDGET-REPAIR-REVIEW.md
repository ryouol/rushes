# Provider budget repair review

Reviewed implementation: `0a11071`. The repair retains the $20 combined monthly target, $1.90 production allowance and $0.85 local allowance. It changes maximum Gemini reservations into holds that settle against measured input/output/thinking tokens. Modal calls and unknown outcomes retain conservative reservations. No provider limit was increased.

Three read-only simplify reviewers covered reuse, quality and efficiency. Four separate final reviewers used the testing, breaking-change, context and change-size skills with xhigh reasoning. Every finding and disposition follows; line references identify the resulting implementation.

1. **P2, simplify quality — rejected-call evidence was not durable.** `backend/rushes/activities.py:503`. Fixed: pending windows now retain the result envelope, reservation ID and rejection evidence. Recovery settles that exact result before a subsequent attempt can replace it (`activities.py:421`).
2. **P3, simplify efficiency — unknown usage opened a redundant database connection.** `backend/rushes/provider_budget.py:113`. Fixed: unsupported or incomplete usage returns before opening a connection. Unknown calls retain their full hold.
3. **P2, final testing — successful responses could permanently retain holds after an accounting outage.** `backend/rushes/activities.py:323`. Fixed: applying a durable response retries settlement before completion. A transient outage retries the saved response without another generation. The new integration test injects an outage, verifies the received checkpoint, and then verifies a single generation and $0.0044 settlement.
4. **P2, final breaking changes — fresh-schema verification still expected 0007.** `scripts/verify_schema.py:37`. Fixed: verification expects 0008 and checks append-only settlement privileges. The verifier passed in a disposable database with all 17 forced tenant tables intact.
5. **P2, final context — zero-cost settlements lacked their original rejection evidence.** `backend/rushes/provider_budget.py:133`. Fixed: settlement retains the bounded HTTP status, rejection/preparation type and outcome. Initial handling and durable recovery share `settle_analysis_result`, preserving identical audit evidence.
6. **Testing coverage limitations, reported with finding 3.** `tests/test_provider_budget.py:394`. The outage integration is added. Direct ledger tests for every Flash price/year boundary, Gemini transport ambiguity, preparation failure and input rejection were not added in this focused repair. Existing SDK rejection tests and ledger unknown-usage tests cover the boundaries separately. Live recovery verifies real successful Pro settlement; it is not evidence for every failure variant.

Reuse review found no concrete issue: provider dollar accounting and internal processing credits intentionally use different units and scopes. Final size review counted 447 changed lines across seven implementation/test/schema files (246 implementation/schema and 201 tests), below the skill's 500-line complex-change threshold. The additional fresh-migration evidence changes no behavior. Context review confirmed unchanged prompt bounds; Rust/Codex-specific skill paths do not apply to this Python application.

Focused isolated validation: 35 tests passed, paid calls disabled; formatting/lint and fresh-schema verification passed. Production recovery and full CI results are recorded in `docs/validation/budget-repair.json`. No pull request exists, so there was no PR to label. No GitHub comments were posted.
