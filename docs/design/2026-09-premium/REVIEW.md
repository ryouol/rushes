# Review record and dispositions

Release series based on `ad8a9ce`, reviewed through the AI organizer and management corrections on 2026-09-11. The user explicitly requested commit and push; implementation is now recorded in coherent commits on `codex/production`. No PR, merge or deployment is claimed. Earlier local-only evidence below remains historical. The requested simplify and code-review skills were applied. This record includes findings even when subsequently fixed; it does not turn a large uncommitted snapshot into an approved single merge unit.

## Numbered findings

1. **Ordinary request bodies unbounded before parsing** — `backend/rushes/security.py:17`. Fixed with 64 KiB ordinary / 8 MiB folder-index limits and exact streaming-upload exception. Regression coverage and complete 49-operation audit in `docs/SECURITY-AUDIT.md`.
2. **Required public assets absent from Docker context/runtime** — `.dockerignore:26`, `Dockerfile:9`. Fixed by including `web/public` in the context and copying it into build/runtime stages. Production web build passes; this turn did not build/deploy a complete Docker image.
3. **Crawler origins could be frozen at build time** — `web/app/robots.ts:4`, `web/app/sitemap.ts:4`. Fixed with dynamic runtime generation; local HTTP reports deliberate local origin from configuration.
4. **Worklog correction draft lost on row remount/collapse** — `web/components/player.tsx:33`. Fixed by storing controlled text/start/end in asset/workspace-scoped draft state, preserving through collapse/filtering, clearing on Save/Cancel/asset change and preventing replacement by another edit. Real browser edit at 13–19 seconds survived collapse and saved; regression asserts remount and different-asset boundaries.
5. **Browser suites still targeted previous routes/control names** — `web/e2e/core.spec.ts:1`, `web/e2e/state-regressions.spec.ts:1`. Updated for public landing/auth routes, Library/Collections, organization responses, category filtering, cancellation and disclosed selection tools. Typechecked/reviewed; CLI execution was not performed because testing stayed in the user's in-app browser. This is a validation limit, not a claim that the suite ran.
6. **Export lists materialized full frozen category evidence** — `backend/rushes/routes_exports.py:326`. Fixed with ordered database JSONB projection returning output/bytes only; full evidence remains in the export record/provenance file. Tests distinguish compact listing from persisted full evidence and verify download order.
7. **Resumed current-schema analysis still displayed partial** — `backend/rushes/organization.py:145`. Fixed by prioritizing active asset states even when the preserved run retains a failed/canceled status. Failed/canceled → retry → queued/processing/preview-ready tests preserve completed memberships and processing counts.
8. **Repeated project category aggregation** — `backend/rushes/organization.py:225`. Fixed by obtaining category_total with count(*) OVER() in the bounded category query, removing the second full grouping query. Existing empty and 100-category-boundary coverage passes.
9. **Category labels normalized twice** — `backend/rushes/inference.py:39`. Both reuse and quality passes reported this. Fixed by calling category_records(values) directly; it owns validation and deduplication.
10. **Repeated organization category type** — `web/components/organization-library.tsx:93`. Fixed by using the exported OrganizationCategory type.
11. **Change set too large for one review/merge unit** — `web/components/project.tsx:1` and the overall worktree. The earlier size pass counted roughly 9,387 implementation/test/CSS changed lines before the organizer additions. **Disposition: retain as a pre-merge staging requirement.** Separate coherent security, shared appearance/dialogs, consent, auth/onboarding/routes, landing/assets, organizer backend+tests, workspace, inspection and documentation slices; keep required imports/types/callers together. No single oversized commit/PR was made.
12. **Download foreground did not follow the dark accent palette** — `web/components/editor.css:102`. Mobile dark browser inspection exposed the mismatch. Fixed by using `--on-accent` for the link foreground alongside `--accent` for its background. Final visual review remains recorded separately; the token change does not by itself establish a complete contrast audit.
13. **Technical output path competed with file downloads** — `web/components/project.tsx:787`. Moved the disk path into a collapsed Output location disclosure, keeping completed-file download links directly visible. This changes presentation only; the persisted path and authenticated download route remain intact.

Line references identify the reviewed area; implementations moved as components were extracted. Findings 1–10 and 12–13 are repaired in the local source. Finding 11 is an integration workflow constraint before an eventual merge, not a runtime defect. Earlier simplify passes also removed unused CSS and repaired duplicated project scans, eager source-root fetches and missing confirmed-creation tracking callers; these are recorded in the implementation files and prior task history.

## Final perspectives and evidence

- Simplify: independent reuse, quality and efficiency passes on the new organizer/landing/player changes; all three concrete cleanup findings repaired.
- Breaking changes: no remaining confirmed finding after compact export-list and resumed-state fixes; subsequent full relevant-diff check also returned none.
- Testing: no actionable finding after regression updates. Significant backend/API, stale-response and draft boundaries have coverage. Manual browser paths supplement tests; they do not represent a CLI Playwright run.
- Model-visible context: no finding. Prompt/schema advance together; transcript/input/output bounds and untrusted-evidence instructions remain. Category/file labels are data. The public example makes no model call.
- Change size: split before merge as described above. No GitHub labeling/commenting applies without a PR.

Recorded backend verification: 156 passed and 1 opt-in Temporal test skipped in the full suite, with paid AI disabled. Subsequent focused organizer/inference/export checks passed 53 tests; later category normalization checks passed 27 tests. The focused runs do not represent a repeated full-suite run. Six no-network analytics tests passed. The final production Next build passed with 791 ms compilation, 2.2 seconds for TypeScript and 14 generated pages. The subsequent local audit scanned 339 source/client-bundle files with zero findings and refreshed the 49-operation inventory. Ruff and whitespace checks passed. Hosted build/secret and infrastructure verification remains a release requirement.

The [synthetic organizer fixture](qa/organizer-fixture-readme.md) was also exercised through the real authenticated category-export API and worker. Waves produced two full-file outputs, a browser download event was observed, and both output byte hashes match the preserved originals. [Export verification](qa/category-export-verification.json) records those files and byte-match results. Its category assignments are deterministic fixture data; this evidence validates copying and downloading those members, not live model classification quality.

The [standalone motion study](motion-study.html) provides user-controlled processing/category, disclosure/dialog/sheet and completion examples, with Light/Dark and Reduce motion controls. It embeds the actual local font, mark and generated demo thumbnails and makes no provider/API requests. Embedded source bytes, script syntax and DOM references passed preparation checks. In-app browser review confirmed stage progression, two Waves entries, the two-copy example receipt, disclosure/dialog/sheet controls, Escape with focus return, theme changes and the manual instant-entrance state. Desktop 1330px and mobile 390px had no horizontal overflow; [MOTION.md](MOTION.md) links the saved views. This remains an illustrative artifact, not a product recording or verification of the actual app's OS-level `prefers-reduced-motion` matrix.

The bounded live AI smoke is separately recorded in [its evidence file](qa/live-ai-category-smoke.json); a conservative request reservation is not a measured invoice. Browser layout/interaction evidence, final build checks and remaining scope limits belong in the final design QA record. No deployment or hosted release clearance is claimed.

At local handoff, the API and worker again inherit the normal key from the unchanged `.env`, with the temporary blank-key test override removed. The environment-only monthly allowance override remains 2,000,000 micro-USD ($2), and the conservative reservation is 60,000 micro-USD ($0.06). Local health returned HTTP 200; no jobs were pending or running and no new inference was requested during restoration. Test-run AI disabling is historical validation context, not the final runtime configuration. The browser fixture retains its deliberately synthetic category evidence.


## Management iteration — final review

14. **Canceled worker threads could recreate deleted files** — `backend/rushes/deletion.py:347`, `backend/rushes/storage.py:154`. Fixed by retaining shared workspace file leases through real thread completion, including repeated cancellation. DELETE returns a useful conflict while work drains. The real prepare-thread cancellation regression passes; Temporal workflow command ordering stays unchanged.
15. **Completed deletion could navigate back after browser Back** — `web/components/delete-dialog.tsx:58`. Fixed with component-lifetime and request-start URL checks before applying completion navigation. The server operation continues, while a newer screen is preserved.
16. **Deletion duplicated search clearing and left its notice behind** — `web/components/project.tsx:339`. Fixed by using the existing clearSearch helper.
17. **Storage size formatting duplicated between settings and upload** — `web/components/import-dialog.tsx:5`, `web/lib/format.ts:1`. Fixed with one shared formatter, including unavailable-value handling.
18. **Failure-test wrapper dropped a required filesystem capability attribute** — `tests/test_deletion.py:640`. The first full run found this fixture bug (193 passed, one failed, one skipped). Fixed with functools.wraps; the final full suite passes, including the partial-removal/retry behavior.
19. **Deletion loaded large frozen export JSON unnecessarily** — `backend/rushes/deletion.py:223`. Fixed by selecting only IDs, state and job references for locking/existence checks. Full plan/provenance records are no longer materialized for project/workspace cleanup.
20. **Deletion confirmation and stale-navigation guards lacked browser regressions** — `web/e2e/state-regressions.spec.ts:182`. Added three focused cases for project/workspace name confirmation, duplicate submissions, retained 409 failures and navigation before a held DELETE completes. Typechecked and reviewed; these browser tests were not executed. Manual in-app confirmation/cancel and real HTTP deletion checks are separately recorded.
21. **Combined snapshot exceeds review-size guidance** — `backend/rushes/api.py:20`. The review counted about 16,500 changed text lines and 104 binary assets. Split into request boundaries, storage reporting, review controls, organization, frozen exports, safe deletion, consent, frontend cutover and evidence commits. Destructive endpoints and their lifetime guards remain together. The frontend cutover remains an explicitly large stage because its route, caller, prop and CSS replacements must ship together; this limitation is not represented as an under-800-line review.
22. **Frontend extraction also changes behavior** — `web/components/project.tsx:42`, `web/components/player.tsx:1`, `web/components/rushes.tsx:1`. The commit keeps required callers and replacement styles together and does not claim a mechanical-change exemption.

All runtime and test-fixture findings above are repaired. Breaking-change and model-context reviews have no remaining findings; reuse, quality and efficiency findings are repaired. Size and browser-test execution limits remain explicit.

Final verification: 194 backend tests passed, one opt-in Temporal test skipped, one dependency deprecation warning (33.53 seconds); paid AI disabled. Six no-network analytics tests passed. Production Next build passed (1,101 ms compilation, 2.2 seconds TypeScript, 14 generated pages); a subsequent typecheck includes the added deletion browser regressions. Ruff and whitespace checks passed. Fresh local security scan: 356 files, zero findings, 53 API operations. See [management verification](qa/management-verification.md) for actual disposable-file deletion, responsive screenshots and storage measurements.
