# Cloud integration review — 2026-09-10

The requested simplify passes and the four code-review skill passes were completed using read-only reviewers. This report preserves their findings and follow-up test defects, including duplicates. It supplements the historical [PRODUCTION-REVIEW.md](PRODUCTION-REVIEW.md) and [REVIEW.md](REVIEW.md); their earlier credential/deployment statements are superseded by [PRODUCTION.md](PRODUCTION.md).

## Findings and dispositions

1. **Simplify / reuse — Duplicate provider deletion behavior.** [provider_files.py:1](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/provider_files.py:1>). Fixed: analyzer and maintenance share deletion handling; an already-deleted 404 is successful cleanup, other failures remain retryable.
2. **Simplify / reuse — Session expiry duplicated across authentication paths.** [auth.py:20](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/auth.py:20>). Fixed: cookie, database strategy and long-running access checks share the seven-day lifetime constant.
3. **Simplify / quality — Local-only copy misrepresented hosted storage and compute.** [project.tsx:959](</Users/royluo/Documents/ChatGPT/Video Clipping/web/components/project.tsx:959>). Fixed: import, settings, account, export and legal copy describe server storage and configured providers.
4. **Simplify / quality — Collection suggestions discarded degraded-search notices.** [project.tsx:1189](</Users/royluo/Documents/ChatGPT/Video Clipping/web/components/project.tsx:1189>). Fixed: retain and render the server notice; browser regression verifies it.
5. **Simplify / quality — Delayed collection refresh could overwrite newer additions.** [project.tsx:1192](</Users/royluo/Documents/ChatGPT/Video Clipping/web/components/project.tsx:1192>). Fixed: abort superseded/unmounted requests and reject obsolete responses. Browser regression verifies request abortion, two additions and duplicate-click protection.
6. **Simplify / efficiency — Upload flush/fsync blocked the event loop.** [routes_media.py:194](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/routes_media.py:194>). Fixed: move durable flush/fsync together into a thread; existing upload timeout, access-revocation and cleanup tests still pass.
7. **Simplify / efficiency — Export plans repeatedly loaded the same assets.** [exports.py:145](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/exports.py:145>). Fixed: one tenant-scoped query loads distinct asset IDs; existing source authorization/fingerprint checks remain.
8. **Simplify / efficiency — Player polling caused unnecessary state updates.** [player.tsx:109](</Users/royluo/Documents/ChatGPT/Video Clipping/web/components/player.tsx:109>). Fixed: preserve unchanged asset/observation references and depend on processing status for polling. Browser regression verifies queued→preview→ready polling, preservation of an unsaved correction, and the saved version update.
9. **Simplify / efficiency — Local embedding inference loaded a model in the API process.** [inference.py:258](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/inference.py:258>). Addressed for cloud deployments: Modal routes both indexing and query embeddings through the same remote interface. Local mode intentionally retains its local model.
10. **Simplify / efficiency — Repeated gzip frame-map scans on long footage.** [source_frames.py:9](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/source_frames.py:9>). Deferred optimization, also recorded in the earlier review. It is bounded in memory and preserves validated frame selection; this pass does not establish representative long-footage throughput.
11. **Final / breaking — Modal processing was not disclosed.** [project.tsx:1019](</Users/royluo/Documents/ChatGPT/Video Clipping/web/components/project.tsx:1019>). Fixed: import/settings/privacy disclose derived audio, worklog text and search queries sent to Modal when enabled. Duplicate of the broader copy finding, retained separately.
12. **Final / breaking — Incomplete-processing flag was dropped by collection suggestions.** [routes_collections.py:157](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/routes_collections.py:157>). Fixed: API and UI preserve the flag and explain that suggestions use currently available evidence. Browser regression covers the warning.
13. **Final / breaking — Valid multilingual notes exceeded Modal's byte limit.** [remote_compute.py:22](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/remote_compute.py:22>). Fixed: the shared limit is 16,000 UTF-8 bytes, accommodating 4,000 Unicode code points accepted by the API. An API→database integration and a real deployed 16,000-byte request passed.
14. **Final / context — Same multilingual limit mismatch.** [remote_compute.py:22](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/remote_compute.py:22>). Fixed; duplicate retained as returned by this reviewer.
15. **Final / context — Inputs cross the 1,000-token manual-review gate.** [inference.py:172](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/inference.py:172>). Reviewed: immutable per-window snapshots, 8,000-byte serialized transcript/evidence limits, 10,000-token preflight and 4,096-token output bound. Live preflight counting passed. No unbounded conversation accumulation was found; Rust-specific core/context structures are not applicable.
16. **Final / context — Successful responses omitted preflight count.** [inference.py:77](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/inference.py:77>). Fixed: `AnalysisResult.preflight_tokens` is retained in the durable response envelope and validation report.
17. **Final / testing — Fake provider clients bypassed real SDK schema serialization.** [test_cloud_compute.py:20](</Users/royluo/Documents/ChatGPT/Video Clipping/tests/test_cloud_compute.py:20>). Fixed: a real Google SDK client with a mock HTTP transport exercises outgoing schema/config serialization and strict local response validation. Live testing found and repaired unsupported provider-schema constraints; strict local bounds remain enforced.
18. **Final / testing — Missing Modal boundary and Unicode integration coverage.** [test_cloud_compute.py:91](</Users/royluo/Documents/ChatGPT/Video Clipping/tests/test_cloud_compute.py:91>). Fixed: tests reject wrong model/batch/dimensions/NaN, oversized inputs and shifted source timestamps, then create and persist an API note through the remote contract. The updated deployment separately passed an actual maximum-length Unicode embedding.
19. **Final / testing — Collection race/warning and player progression needed regressions.** [state-regressions.spec.ts:75](</Users/royluo/Documents/ChatGPT/Video Clipping/web/e2e/state-regressions.spec.ts:75>). Added meaningful request-controlled tests for concurrent additions, duplicate clicks, warnings and an open processing player retaining human edits. All pass.
20. **Final follow-up / testing — Stale-response assertion could pass before the response settled.** [state-regressions.spec.ts:166](</Users/royluo/Documents/ChatGPT/Video Clipping/web/e2e/state-regressions.spec.ts:166>). Fixed: explicitly observe the held GET's `ERR_ABORTED` after the newer refresh, then release it and assert retained rows. The old implementation would fail the abortion assertion.
21. **Final follow-up / testing — Later player polls were not observed.** [state-regressions.spec.ts:252](</Users/royluo/Documents/ChatGPT/Video Clipping/web/e2e/state-regressions.spec.ts:252>). Fixed: wait for actual polling responses and rendering, inspect the ready-state response, and verify the draft survives before saving version two. The test uses real polling intervals.
22. **Final / change size — Initial application exceeds review-size guidance.** [pyproject.toml:1](</Users/royluo/Documents/ChatGPT/Video Clipping/pyproject.toml:1>). Open reviewability limitation. The reviewer snapshot contained 128 files / 23,916 lines, 18,047 after excluding lockfiles and validation reports; later tests/docs add to that snapshot. With no HEAD, staged files are new code too. The smallest coherent landing stage is `timing.py` + `test_timing.py` (161 lines), with minimal package/Pydantic/pytest setup. Defer the eager API import in `tests/conftest.py`. Next add pure source-frame/interchange code and tests (285 more lines). Then stage persistence/security, media/workflows, API, UI, Modal, Gemini and hosting changes with their actual dependencies and tests. No PR exists to label and no GitHub comments were posted.

23. **Workflow / testing — Verified-TLS bootstrap lacked a trust root.** [bootstrap_workflow_db.py:29](</Users/royluo/Documents/ChatGPT/Video Clipping/scripts/bootstrap_workflow_db.py:29>). Fixed: libpq explicitly selects system or mounted CA trust; the SQL tool and Temporal share normalized TLS configuration. A real verified-TLS PostgreSQL proxy test passed bootstrap twice and live workflow processing.
24. **Workflow / breaking — Same default TLS bootstrap failure.** [bootstrap_workflow_db.py:29](</Users/royluo/Documents/ChatGPT/Video Clipping/scripts/bootstrap_workflow_db.py:29>). Fixed as above. This duplicate P1 finding is retained separately from the testing review.
25. **Workflow / testing — Shutdown ignored Temporal readiness.** [serve.py:59](</Users/royluo/Documents/ChatGPT/Video Clipping/scripts/serve.py:59>). Fixed: readiness observes termination and the supervisor checks again before spawning workers. Actual startup with unavailable PostgreSQL terminates within the bounded shutdown period, with no public service or worker launched.
26. **Workflow / breaking — Same readiness termination defect.** [serve.py:59](</Users/royluo/Documents/ChatGPT/Video Clipping/scripts/serve.py:59>). Fixed; duplicate retained separately. Normal and active-processing termination, plus child crashes, now have container evidence.
27. **Workflow / simplify reuse — TLS option normalization was duplicated.** [workflow_service.py:54](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/workflow_service.py:54>). Fixed: server configuration and schema-tool environment mapping use one helper; libpq-specific query conversion remains explicit.
28. **Workflow / simplify quality — Host aliases bypassed database separation.** [workflow_service.py:28](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/workflow_service.py:28>). Fixed: the documented shared-Postgres topology requires three distinct database names, independent of host aliases or capitalization. Focused regressions cover aliases.
29. **Workflow / simplify efficiency — Worker queues drained sequentially.** [worker.py:146](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/worker.py:146>). Fixed: both queues stop polling and begin draining concurrently within the supervisor's existing grace period. A regression blocks drain until both queues have stopped polling; the previous sequential behavior fails it. The installed SDK explicitly permits repeated shutdown calls from context exit.
30. **Workflow / testing — Active-media fixture does not create partial FFmpeg output.** [held_ffmpeg.py:15](</Users/royluo/Documents/ChatGPT/Video Clipping/tests/fixtures/held_ffmpeg.py:15>). Evidence limitation retained: the fixture holds the invocation before executing FFmpeg. It proves interrupted activity and same-job recovery, not cleanup after a decoder has written a partial output. Reports state that limit; earlier source-recovery evidence remains separate.
31. **Workflow / change size — Initial landing still exceeds size guidance.** [pyproject.toml:1](</Users/royluo/Documents/ChatGPT/Video Clipping/pyproject.toml:1>). Open reviewability limitation. The 22:49:21 UTC snapshot counted 140 files / 25,520 lines: 124 authored files with 19,486 lines, 2 lockfiles with 4,295 lines, and 14 JSON reports with 1,739 lines. Later edits are excluded. The proposed timing-first split in finding 22 remains. After the application foundation, the worker-drain change plus its 50-line regression can land independently; workflow configuration/bootstrap/installer and supervisor/container verification follow in coherent stages.
32. **Workflow / breaking follow-up — Encoded database names bypassed separation.** [workflow_service.py:29](</Users/royluo/Documents/ChatGPT/Video Clipping/backend/rushes/workflow_service.py:29>). Fixed: database names containing percent encoding are rejected before client-specific parsing can turn different strings into the same database. Regressions cover encoded application and workflow names. The final built image includes this guard; its installed module hash matches the reviewed source. Follow-up breaking review found no further issues.

## Current verification

- Backend: **91 passed, 1 skipped** in 28.68 seconds. The skipped opt-in real Temporal test separately passed in 16.38 seconds. The later encoded-name validation guard passed four focused workflow tests. One upstream Hugging Face deprecation warning.
- Next production build, current TypeScript verification and Ruff passed. Fresh migrations passed at version 0004 with 17 forced-RLS tables. Source/client-bundle audit scanned 158 files and 48 API routes with zero configured-secret or credential-pattern findings.
- Browser with Gemini disabled: **7 passed** in 54.7 seconds. One real API/worker account→upload→worklog→select→rendered export journey and six explicitly synthetic state/evidence regressions.
- Actual private Modal speech and embeddings passed, including the deployed maximum-length Unicode boundary. The app subsequently returned to zero active tasks.
- One actual Gemini video analysis passed. The separate full live-provider browser run failed on Google HTTP 503; RUSHES preserved useful partial results and avoided an automatic repeat. This is an open external/integration validation limit, not a passing production test.
- Local TLS container verification passed: secure sessions, static assets, isolation, trusted identity handling, a 346.24-second upload exceeding the default Node deadline, restart persistence and graceful shutdown. See [hosting-container.json](validation/hosting-container.json). This image is arm64 at 1 GiB / 1 CPU; the attempted amd64 rebuild was interrupted by Docker I/O failure. This does not verify Render ingress or the complete workflow topology.

- New hosted workflow component: real TLS schema bootstrap twice, Postgres workflow-history recovery after container recreation, interrupted activity and same-job resumption, source/session/preview preservation, loopback isolation, startup termination and child-crash shutdown passed. See [workflow-hosting.json](validation/workflow-hosting.json). The final recorded run uses the built amd64 image with no production source or binary mounts. Peak container memory was 672,886,784 bytes (about 642 MiB); active-media shutdown took 4.35 seconds, startup termination 15.67 seconds, and worker/Temporal crashes closed the public service in 1.69/0.55 seconds. These are synthetic local checks, not Render capacity evidence. The context reviewer found no new model-context changes or findings in this stage. Follow-up testing/breaking reviewers confirmed the original TLS/readiness fixes.

**Production remains incomplete.** Render resources, representative capacity, verified ingress, provider-dollar controls and a successful complete cloud journey remain outstanding. The user's $20/month hard cap is unchanged while the budget question is pending. No publication or review artifact is evidence of a deployed public application.
